let no = 7.7
// console.log(Math.round(no)) // IM returns the nearest int
// console.log(Math.floor(no)) // IM round to down
// console.log(Math.ceil(no)) // IM round to up
// console.log(Math.trunc(no)) // IM remove the decimal part

let ran = Math.random()
console.log(Math.floor(ran * 100))